import { Page } from "../core/Page";
import { RepositoryImporter } from "../import/RepositoryImporter";

export const Import = () => {
  return (
    <Page>
      <RepositoryImporter />
    </Page>
  );
};
